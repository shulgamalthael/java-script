function squareArray(array) {
    if (!Array.isArray(array)) {
        return null;
    }

    let array = [];
    for ( let i = 0; i < array.length; i++) {
        return array[i] * array[i];
    }
    return array();
}
function checkSum(arr) {
    if(!Array.isArray(arr)) return null;

    sum = 0;
    let checkSum = [];
    for (let i = 0; i < checkSum.length; i++) {
        sum = sum += checkSum[i];
    }

    return (sum > 100);

}
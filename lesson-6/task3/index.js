function getSpecialNumbers(m,n) {
    let getOddarr = array();
    for( let i = 0; i < array.length; i++) {
        if ( i % 3 === 0) {
            return array;
        }
        return array;
    }
}
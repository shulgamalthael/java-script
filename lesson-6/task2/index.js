function getSum(arr) {
    if (!Array.isArray(arr)) {
        return null;
    }

    let sum = 0;
    const arraySum = array();
    for (let i = 0; i <= array.length; i ++) {
        sum += i;
    }
    return sum;
}